import { HomeScreen } from '@/modules/curriculum/1-presentation/controller/home-screen';

/** Thin Expo Router shell - the screen itself lives in the curriculum module. */
export default function HomeRoute() {
  return <HomeScreen />;
}
